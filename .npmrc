# Resolve the @forkzero scope from public npm. Without this, a developer's
# global ~/.npmrc that maps @forkzero to GitHub Packages would 404 on install.
@forkzero:registry=https://registry.npmjs.org
